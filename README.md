Docker image with common tools used to build Debian packages

- build-essentials
- devscripts
- debhelper
- git
- cmake

and some project-specific ones:

- bison
- flex
- libssl-dev 
- libpcap-dev


Also, `contrib` and `non-free` repos are enabled.
